/**
 * HTTP router for the example app.
 *
 * HTTP routes for the Firecrawl Scrape component will be added
 * when the component's HTTP functionality is implemented.
 */

import { httpRouter } from "convex/server";

const http = httpRouter();

// HTTP routes will be registered here when implemented

export default http;
